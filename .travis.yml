language: python
cache: pip

python:
  - "3.6"

install:
  - pip install -r requirements.txt
  - pip install mypy
  - pip install .

script:
  - mypy -m pynotstdlib
  - python3 -m unittest discover -v -s test